import io.mindspice.toastit.calendar.Calendar;
import io.mindspice.toastit.calendar.CalendarCell;
import org.junit.Test;

import java.time.Month;
import java.time.YearMonth;
import java.util.List;


public class CalendarTests {

    Calendar calendar = new Calendar();

    @Test
    public void testCalendar(){
//        var cellPair = Calendar.generateCalenderCells(YearMonth.of(2024, Month.FEBRUARY));
//        var cells = cellPair.second().stream().map(c -> c.withItems(List.of("\uD83E\uDDB7 10:30 - Dentist", "Lena Concert | 14:30", "No School | 07:00"))).toList();
//        var header = Calendar.createHeaderRow();
//        System.out.println(header);
//        var table = Calendar.createDayCells(cellPair.first(), cells, null );
//        System.out.println(table);


    }
}
